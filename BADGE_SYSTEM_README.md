# 🏅 Система Баффов и Верификации v1.0

## 📋 Обзор

Автоматическая система проверки профилей участников главного сервера (Sunshine Paradise) с присвоением мультипликаторов к монетам.

**Главный сервер ID:** `1168585868882215004`

---

## 🎯 Уровни Баффов

### 1️⃣ Basic (1.10x множитель)
- **Условие:** Наличие тега гильдии в нике **ИЛИ** ссылки на сервер в описании
- **Роль:** `1475882293733363826`
- **Действие:** +10% к монетам за сообщения

### 2️⃣ Verified (1.25x множитель)
- **Условие:** Тег гильдии **И** ссылка на сервер + тег в описании
- **Роль:** `1475884914359406702`
- **Действие:** +25% к монетам за сообщения

---

## 🔄 Как Это Работает

### Проверка Профиля
```python
async def check_member_profile(member: disnake.Member) -> Optional[str]:
    # 1. Проверяем ник на тег [NAME]
    # 2. Проверяем описание на discord.gg ссылку
    # Возвращает: "verified" | "basic" | None
```

### Применение Баффа
Мультипликатор автоматически применяется в `on_message`:
```python
badge_multiplier = get_member_badge_multiplier(msg.author.id)
co_gain = int(COINS_PER_MSG * badge_multiplier)
```

### Автоматические Процессы

#### 1. Фоновая Задача (каждые 30 минут)
```
verify_member_badges_task() 
├─ Проверяет 50 участников за итерацию
├─ Обновляет кэш баффов
└─ Автоматически присваивает/удаляет роли
```

#### 2. При Вступлении на Главный Сервер
```
on_member_join()
├─ Проверяет профиль нового участника
├─ Обновляет кэш
└─ Присваивает роль если есть бафф
```

---

## 💾 Интеграция с Гильдиями

### При Создании Гильдии
```python
# Создаётся роль для гильдии
guild_role = await create_guild_role(guild, tag, color)

# Сохраняется в БД
save_guild(gid, {"guild_role_id": guild_role.id})

# Лидер получает роль
await ctx.author.add_roles(guild_role)
```

### При Вступлении в Гильдию
```python
# Участник получает роль гильдии
await invited.add_roles(guild_role)
```

### При Выходе/Кике
```python
# Роль удаляется
await member.remove_roles(guild_role)
```

---

## 🎮 Команды

### Пользовательские
- **`!mybadge`** — Показать текущий статус баффа
- **`!mybadge @user`** — Проверить статус другого участника

### Административные
- **`!badgestatus @user`** — [Админ] Подробная инфо о профиле
- **`!verifyall`** — [Владелец] Проверить всех участников главного сервера
- **`!applyretro`** — [Владелец] Применить роли гильдий ко всем существующим членам

---

## 🔧 Конфигурация

```python
HOME_SERVER_ID = 1168585868882215004

MEMBER_BADGES = {
    "basic": {
        "role_id": 1475882293733363826,
        "multiplier": 1.10,
        "description": "Тег гильдии",
        "emoji": "🏷️",
    },
    "verified": {
        "role_id": 1475884914359406702,
        "multiplier": 1.25,
        "description": "Полный профиль сервера",
        "emoji": "✅",
    },
}
```

**Добавить новый бафф:**
```python
MEMBER_BADGES["premium"] = {
    "role_id": 1234567890,
    "multiplier": 1.50,
    "description": "Премиум участник",
    "emoji": "⭐",
}
```

---

## 📊 Кэширование

- **TTL:** 1 час (3600 секунд)
- **Обновляется:** Каждые 30 минут в фоне
- **Структура:** `{member_id: (badge_level, multiplier, timestamp)}`

Старевший кэш автоматически очищается фоновой задачей.

---

## 🛠️ API Функции

### `check_member_profile(member) -> Optional[str]`
Проверяет профиль и возвращает уровень баффа.

### `get_member_badge_multiplier(member_id) -> float`
Получает мультипликатор из кэша (по умолчанию 1.0).

### `create_guild_role(srv, tag, color) -> Optional[disnake.Role]`
Создаёт роль для гильдии.

---

## 📝 Примеры

### Проверить Статус
```
!mybadge
→ 🏷️ Тег гильдии
→ Мультипликатор: x1.10
```

### Подробная Информация
```
!badgestatus @User
→ Ник: [SP] Username
→ Bio: discord.gg/example [SP]
→ Статус: ✅ Полный профиль сервера
→ Мультипликатор: x1.25
→ Роль: @Verified Badge
```

### Ретроактивное Применение
```
!applyretro
→ ⏳ Обработка существующих гильдий...
→ ✅ Завершено!
→ Создано ролей: 15
→ Применено участникам: 128
```

---

## 🐛 Отладка

### Проверить Кэш
```python
from guilds import _member_badge_cache
print(_member_badge_cache)
```

### Форсировать Обновление
```
!verifyall  # Проверить всех на главном сервере
!applyretro # Применить к гильдиям
```

### Логи
Все операции логируются с префиксом:
- `[check_member_profile]`
- `[verify_member_badges_task]`
- `[on_member_join]`
- `[gcreate]`
- `[invite:accept]`
- и т.д.

---

## 🔐 Безопасность

- **Rate Limit:** 0.2 сек между операциями с ролями
- **Проверки прав:** Администраторские команды требуют `@is_owner()`
- **Обработка ошибок:** Все исключения логируются

---

## 📦 Зависимости

- `disnake` — Discord API
- `pymongo` — Для сохранения guild_role_id
- `asyncio` — Для фоновых задач

---

## 🚀 Будущие Улучшения

- [ ] Кастомизация проверки профиля (regex patterns)
- [ ] Web-dashboard для статистики баффов
- [ ] Temporal rewards (повышенные баффы в определённое время)
- [ ] Интеграция с XP системой (баффы к XP)
- [ ] Награды за достижение уровней баффа

